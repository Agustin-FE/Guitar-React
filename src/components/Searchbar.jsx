import React from "react";

const SearchBar = () => {
  return (
    <div>
      <input
        className="input is-primary"
        type="text"
        placeholder="Primary input"
      ></input>
    </div>
  );
};

export default SearchBar;
